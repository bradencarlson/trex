# Short Example

This example was included to test short config files, since these presented an
issue at one point. Also this example serves to test the multiple passes feature
of TreX. This document should compile twice, since there is a reference.
